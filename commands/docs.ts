/**
 * Docs command - parent for the documentation site
 *
 * Opens a sub menu to run or deploy the docs site.
 */

import { defineCommand } from '@pokit/core';

export const command = defineCommand({
  label: 'Documentation site',
});
